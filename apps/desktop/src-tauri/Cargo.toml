[package]
name = "oas-desktop"
version = "0.9.0"
description = "OAS Local Studio 0.9 desktop shell"
edition = "2021"

[dependencies]
tauri = { version = "2.0.0", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }
